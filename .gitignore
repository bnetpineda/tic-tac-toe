# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
# Ignore node_modules within the frontend and backend folders
/frontend/node_modules
/backend/node_modules
# Yarn PnP
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# Frontend specific (Next.js in this case)
/frontend/.next/
/frontend/out/

# Production build outputs
# If your frontend has a build directory like 'build' in the frontend folder:
/frontend/build/
# If your backend has a build or distribution directory (adjust path as needed):
# /backend/dist/
# /backend/build/

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
# Ignore .env files in both frontend and backend folders
/frontend/.env*
/backend/.env*

# vercel (frontend specific deployment)
.vercel

# typescript (frontend specific if backend is not TS)
/frontend/*.tsbuildinfo
/frontend/next-env.d.ts

# Add any other backend-specific ignores here if needed
# e.g., logs, temporary files, database files etc.
# /backend/logs/
# /backend/*.log
node_modules
